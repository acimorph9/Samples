<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <div class="input">
    <%= f.text_field :name %>
    </div>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <div class="input">
      <%= f.text_field :email %>
    </div>
  </div>
  <div class="actions">
    <%= button_tag "Create User", :class => "btn success" %>
  </div>
<% end %>
